<h1>List of all directors</h1>

<table border="1">
<% Director.all.each do |dir| %>

  <tr>
  <td> <%= dir.id %></td>
   <td> 
    <img source = "<%= dir.image%>">
   </td>
    <td><%= dir.name %></td>
    <td><%= dir.dob %></td>
    <td> <a href = '/directors/<%= dir.id %>'>Show details </a></td>
  </tr>

<%end%>

</table>